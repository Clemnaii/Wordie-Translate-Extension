# 项目结构说明

## 📁 目录和文件用途

### 🎯 核心源码目录

#### `src/` - 源代码目录
```
src/
├── manifest.json          # Chrome 扩展配置文件（告诉 Chrome 如何加载扩展）
├── content/               # Content Script（注入到网页的脚本）
│   ├── index.tsx         # 主逻辑：文本选择、图标显示等
│   └── index.css         # Content Script 的样式
└── popup/                 # 扩展弹窗（点击扩展图标时显示的界面）
    ├── index.html        # 弹窗 HTML
    ├── index.tsx         # 弹窗 React 组件
    └── index.css         # 弹窗样式
```

**作用**：你写的源代码，Chrome 不能直接运行，需要编译。

---

### 🔨 构建输出目录

#### `dist/` - 编译后的文件（类似后端的 build/）
```
dist/
├── manifest.json          # 编译后的 manifest（Chrome 实际读取的）
├── src/
│   ├── content/
│   │   ├── index.tsx.js  # 编译后的 JavaScript（TypeScript → JS）
│   │   └── index.css.js  # 编译后的 CSS
│   └── popup/
│       └── index.html    # 编译后的 HTML
└── vendor/               # 第三方依赖（React、Vite 等）
```

**作用**：Chrome 实际加载的文件。**可以删除，运行 `npm run dev` 或 `npm run build` 会重新生成。**

---

### ⚙️ 配置文件

#### `package.json` - 项目配置和依赖
- **作用**：定义项目名称、版本、依赖包、运行脚本
- **关键脚本**：
  - `npm run dev`：启动开发服务器，实时编译
  - `npm run build`：构建生产版本

#### `vite.config.ts` - Vite 构建配置
- **作用**：告诉 Vite 如何编译代码（使用 React、CRXJS 插件等）

#### `tsconfig.json` - TypeScript 配置
- **作用**：TypeScript 编译选项（类型检查、ES 版本等）

#### `tailwind.config.js` - Tailwind CSS 配置
- **作用**：Tailwind CSS 样式框架配置

#### `postcss.config.js` - PostCSS 配置
- **作用**：CSS 处理工具配置（配合 Tailwind）

---

### 📦 依赖目录

#### `node_modules/` - npm 安装的依赖包
- **作用**：所有第三方库（React、Vite、TypeScript 等）
- **大小**：通常很大（几百 MB）
- **可以删除**：运行 `npm install` 会重新安装

#### `package-lock.json` - 依赖版本锁定文件
- **作用**：锁定依赖的精确版本，确保团队使用相同版本

---

### 📝 文档文件

#### `README.md` - 项目说明文档
#### `QUICKSTART.md` - 快速启动指南
#### `icons/README.md` - 图标说明

---

### 🗑️ 其他文件

#### `index.html` - 开发时的入口文件（Vite 用）
- **作用**：开发模式下的 HTML 入口（Chrome 扩展不使用）

#### `.gitignore` - Git 忽略文件配置
- **作用**：告诉 Git 哪些文件不需要提交（如 `node_modules/`、`dist/`）

---

## 🔄 文件流转过程

```
源代码 (src/)
    ↓
Vite 编译 (vite.config.ts)
    ↓
编译后的文件 (dist/)
    ↓
Chrome 加载 (dist/)
```

## 📊 类比后端开发

| 前端（这个项目） | 后端（如 Spring Boot） |
|----------------|---------------------|
| `src/` | `src/main/java/` |
| `dist/` | `target/` 或 `build/` |
| `vite.config.ts` | `pom.xml` 或 `build.gradle` |
| `package.json` | `pom.xml` 或 `build.gradle` |
| `node_modules/` | `.m2/repository/` (Maven) |

