# Backend Configuration (Vercel Serverless Function)
# These variables should be set in Vercel Project Settings > Environment Variables
# For local development (vercel dev), copy these to your .env file

# --- API Keys (Choose at least one) ---
# OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=...
# DEEPSEEK_API_KEY=...
# ALIBABA_API_KEY=...

# --- Default Behavior ---
# Default provider when frontend requests "proxy" mode
# Options: openai, gemini, deepseek, alibaba
DEFAULT_PROVIDER=alibaba

# Default model for the provider
# e.g., gpt-4o, gemini-1.5-pro, deepseek-chat, qwen-turbo
DEFAULT_MODEL=qwen-turbo
